<% content_for :body do %>
  <ul id="members">
    <% @items.each do |item| %>
      <li>
        <%= link_to item.name, refinery.members_group_item_path(item.group, item) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
